processing.setup = function () {
	size(200, 200);
	background(255);
	fill(0);

	noLoop();

	PFont fontA = loadFont("Times");
	textFont(fontA, 16);
	textAlign(CENTER);
}

processing.draw = function () {
	text("Hello World!", processing.width/2, processing.height/2);
}
