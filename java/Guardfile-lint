guard :shell do
  watch(/(.*\.xml)|(.*\.java)/) do |m|
    title = 'Lint'
    eager 'checkstyle -c checkstyle.xml **/*.java'
    status = ($?.success? && :success) || :failed
    n '', title, status
    ''
  end
end
