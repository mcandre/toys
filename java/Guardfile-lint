guard :shell do
  watch(/checkstyle\.xml|.*\.java/) do |m|
    title = 'Lint'
    msg = `checkstyle -c checkstyle.xml **/*.java`
    status = ($?.success? && :success) || :failed

    n msg, title, status
    "-> #{msg}"
  end
end
