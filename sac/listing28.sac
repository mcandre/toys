use StdIO: all;
use Array: all;

int main() {
	v = [1, 2, 3, 4, 5, 6, 7, 8, 9];
	print(v);

	w = [v, v + 10, v + 20];
	print(w);

	x = where( w % 2 == 0, w, -w);
	print(x);

	print(max(x, 0));

	return(0);
}