#!/bin/sh
#
# Install:
# cp sample.envrc .envrc
#
# Uninstall:
# rm .envrc

source_up

# shellcheck source=/dev/null
. "$HOME/.bashrc.d/direnv/chruby.sh"

# shellcheck source=/dev/null
. "$HOME/.bashrc.d/direnv/pyenv.sh"
