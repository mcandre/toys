---
name: "Lint"
on:
  push:
  pull_request:
jobs:
  Lint:
    runs-on: "ubuntu-latest"
    steps:
    - uses: "actions/checkout@v4"
    - run: "apt-get install -y golang rust"
    - run: "make -f install.mk"
    - run: "make lint"
