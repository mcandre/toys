---
name: "Lint"
on:
  push:
  pull_request:
jobs:
  Lint:
    runs-on: "ubuntu-latest"
    steps:
    - uses: "actions/checkout@v4"
    - run: "sudo apt-get install -y cargo golang make"
    - run: "make -f install.mk"
    - run: "make lint"
